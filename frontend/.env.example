# Production (Vercel): set this in Project → Settings → Environment Variables
# Use your Render URL with NO trailing slash
VITE_API_URL=https://cinescope-21yg.onrender.com

# Local dev: leave unset — Vite proxies /api → http://127.0.0.1:8000
